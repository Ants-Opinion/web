# 부정적 반응 데이터 미표시 원인 분석

## 문제 상황
반도체 섹터, 오늘 날짜(2025-11-29)에 대한 부정적 의견의 "더보기" 버튼 클릭 시 팝업에 데이터가 표시되지 않음.

## 현재 필터링 로직

### 부정적 반응 필터링 조건
```typescript
case 'negative':
  shouldIncludeChannel = channelScore < 40;  // 40점 미만
  break;
```

- **긍정적 반응**: `channelScore >= 70` (70점 이상)
- **부정적 반응**: `channelScore < 40` (40점 미만)
- **중립적 반응**: `40 <= channelScore < 70` (40점 이상 70점 미만)

## 가능한 원인

### 1. 필터링 조건 문제
- 모든 채널의 score가 40 이상일 경우 부정적 반응으로 분류되는 채널이 없음
- 실제 데이터에는 부정적 반응이 있지만, 채널 score가 40 이상이어서 필터링되지 않음

### 2. 데이터 구조 문제
- Firebase 데이터베이스의 `detail_dates` 문서 구조가 예상과 다를 수 있음
- `posts` 필드가 없거나 빈 객체일 수 있음
- `score` 필드가 없거나 잘못된 형식일 수 있음

### 3. 날짜 매핑 문제
- 선택된 날짜(`2025-11-29`)에 대한 데이터가 `detail_dates` 컬렉션에 존재하지 않을 수 있음
- 날짜 형식이 일치하지 않을 수 있음

## 추가된 디버깅 로그

다음 정보들이 콘솔에 출력됩니다:

1. **필터링 시작 로그**
   - `=== negative 반응 필터링 시작 ===`

2. **각 채널별 정보**
   - 채널명, score 값, posts 존재 여부
   - 조건 만족 여부 및 기준
   - posts 키 개수 및 키 목록
   - 추가된 포스트 수

3. **필터링 결과 요약**
   - 전체 채널 수
   - posts가 있는 채널 수
   - 조건을 만족하는 채널 수
   - 최종 필터링된 포스트 수

## 확인 방법

1. **브라우저 콘솔 확인**
   - "더보기" 버튼 클릭 시 콘솔에 출력되는 로그 확인
   - 각 채널의 score 값 확인
   - 조건을 만족하는 채널이 있는지 확인

2. **Firebase 데이터베이스 확인**
   - 경로: `/sector_detail/반도체/detail_dates/2025-11-29`
   - 각 채널의 `score` 값 확인
   - 각 채널의 `posts` 구조 확인

3. **필터링 조건 검증**
   - 실제로 부정적 반응이 있는 채널의 score가 40 미만인지 확인
   - 필요시 필터링 조건 조정 고려

## 다음 단계

1. 콘솔 로그를 통해 실제 채널들의 score 값 확인
2. Firebase 데이터베이스에서 실제 데이터 구조 확인
3. 필터링 조건이 적절한지 검토
4. 필요시 필터링 조건 조정 또는 데이터 구조 수정




